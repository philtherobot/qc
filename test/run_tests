if [[ "$1" == "" ]]
then
  echo "usage: run_tests [-v] <path>"
  echo "  -v   verbose"
  echo 
  exit 1
fi

if [[ $1 == -v ]]; then shift; export verbose=1; fi

here=`dirname $0`
export RUNTEST_ROOT=`readlink -f $here`
export QC=`readlink -f $here/../src/qc`

$1/_tests


